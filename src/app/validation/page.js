/* File & Folder to Be Removed */

const Validation = () => {
    return (
        <div>
            <h1>Validation of tasks (if this will be a route.)</h1>
        </div>
    );
};
export default Validation;